<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'profile-trends' %>
      <%= render 'feed' %>
      <%= render 'who-to-follow' %>
    </div> 
  </div>
</section>